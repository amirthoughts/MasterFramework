browser=chrome

#local or remote
run.mode.browser = local

run.mode.mobile = local

#selenium, selenium, browser_stack
browser.remote.mode = selenoid

#browser_stack, sauce_labs
mobile.remote.mode = browser_stack

#remote url where selenium grid is present
selenium.grid.url = http://localhost:4444/wd/hub

selenoid.url = http://localhost:4444/wd/hub